require 'spec_helper'

describe NaviClient do
  it 'has a version number' do
    expect(NaviClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
